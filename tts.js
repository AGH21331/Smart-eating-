const MODEL=process.env.GEMINI_TTS_MODEL||'gemini-3.1-flash-tts-preview';
function wav(b64){const pcm=Buffer.from(b64,'base64');const h=Buffer.alloc(44);h.write('RIFF',0);h.writeUInt32LE(36+pcm.length,4);h.write('WAVE',8);h.write('fmt ',12);h.writeUInt32LE(16,16);h.writeUInt16LE(1,20);h.writeUInt16LE(1,22);h.writeUInt32LE(24000,24);h.writeUInt32LE(48000,28);h.writeUInt16LE(2,32);h.writeUInt16LE(16,34);h.write('data',36);h.writeUInt32LE(pcm.length,40);return 'data:audio/wav;base64,'+Buffer.concat([h,pcm]).toString('base64')}
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const key=process.env.GEMINI_API_KEY;if(!key)return res.status(503).json({error:'AI is not configured on the server yet'});const b=typeof req.body==='string'?JSON.parse(req.body):(req.body||{});const text=String(b.text||'').trim().slice(0,2500);if(!text)return res.status(400).json({error:'Text required'});const language=b.language==='fr'?'French':b.language==='en'?'English':'Arabic';const prompt=`Speak naturally in ${language}. Warm, clear nutrition-coach voice. Read only this text:\n${text}`;const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${MODEL}:generateContent`,{method:'POST',headers:{'Content-Type':'application/json','x-goog-api-key':key},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{responseModalities:['AUDIO'],speechConfig:{voiceConfig:{prebuiltVoiceConfig:{voiceName:'Kore'}}}}})});const d=await r.json();if(!r.ok)return res.status(502).json({error:d?.error?.message||'TTS failed'});const data=d?.candidates?.[0]?.content?.parts?.find(p=>p.inlineData?.data)?.inlineData?.data;if(!data)throw Error('No audio returned');res.setHeader('Cache-Control','no-store');return res.status(200).json({audio:wav(data)})}catch(e){console.error(e);return res.status(500).json({error:e?.message||'TTS server error'})}}
